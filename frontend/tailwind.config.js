module.exports = {
  // ...existing config
  theme: {
    extend: {
      colors: {
        golden: '#FFD770',
      },
      animation: {
        'pulse-slow': 'pulse 3s cubic-bezier(0.4, 0, 0.6, 1) infinite',
      },
    },
  },
}